seed: 42

experiment:
  name: default

device: auto

dataset:
  image_size: 224
  num_workers: 4
  pin_memory: true
  batch_size: 32
  persistent_workers: true

logging:
  level: INFO

output:
  save_dir: experiments